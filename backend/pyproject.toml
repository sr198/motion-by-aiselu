[project]
name = "backend"
version = "0.1.0"
description = "Backend for Motion by Aiselu - Physiotherapy dictation app"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-adk>=1.4.1",
]
